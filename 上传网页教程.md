# 上传网页教程：把 CHM 转换结果放到网上

> 适用对象：你已经在本地用 `chm-web` 把 `.chm` 转成了静态站（得到了一个输出目录，里面有 `index.html`、`__chm_nav.html`、`keywords.json` 和一堆真实 `.htm` 页面），现在想把它放到网上，让别人用手机 / 电脑能直接打开。
>
> **这套产物 100% 是纯静态文件**，不依赖任何后台程序，所以可以免费扔到任何「静态托管」平台。下面给出最常见的两种方式：**GitHub Pages**（免费）和 **Cloudflare Pages**（免费）。

---

## 一、先说结论：三种上传方式怎么选

| 方式 | 难度 | 是否免费 | 适合 |
|------|------|----------|------|
| **A. GitHub Pages** | ★☆☆ | 免费 | 公开站点；你已会 Git；想顺便版本管理 |
| **B. Cloudflare Pages** | ★★☆ | 免费 | 要更快、或想要私有/密码保护的进阶玩法 |
| **C. 自己服务器 / 网盘直链** | ★★★ | 看情况 | 已有域名、服务器，或只是临时分享给几个人 |

下面按 A、B 顺序给详细步骤。**选一种做即可，不用全做。**

---

## 二、方式 A：GitHub Pages（推荐，免费公开）

### 前提
- 一个 GitHub 账号。
- 电脑已能用 Git 推送（你之前已经配好并用过：`git push` 到 GitHub）。

### 步骤

1. **把转换输出目录复制出一份干净的**（可选）。
   例如你在 `out\7z-demo` 得到了站点，就整包留着。它要作为「网站根目录」上传。

2. **在 GitHub 上新建一个仓库**。
   - 打开 https://github.com/new
   - Repository name 随便填，比如 `my-chm-site`。
   - 建议选 **Public（公开）**，因为 GitHub Pages 免费版只支持公开站点；如果偏要私有，见末尾「私有部署」提示。
   - 不要勾选 "Add a README"，直接 Create。

3. **把本地站点推到这个仓库**。
   在这台电脑打开 PowerShell，进到你的站点输出目录，执行：

   ```powershell
   cd "C:\Users\qiujian.shi\Desktop\chm-web\out\7z-demo"   # 换成你实际的输出目录
   git init
   git add -A
   git commit -m "deploy: chm site" 2>&1
   git branch -M main
   git remote add origin https://github.com/你的用户名/my-chm-site.git
   git push -u origin main
   ```

   > 如果你的站点目录里已经有 `index.html`，就非常稳，GitHub Pages 会自动把根目录的 `index.html` 当入口。

4. **打开 Pages 开关**。
   - 进到刚建的仓库 → **Settings（设置）** → 左侧 **Pages**。
   - Source（发布源）选 **Deploy from a branch**，分支选 **main**，目录 `/ (root)`。
   - 点 **Save**。

5. **拿到访问地址**。
   等 1~2 分钟构建完成后，页面顶部会出现一行绿条，例如：
   `Your site is published at https://你的用户名.github.io/my-chm-site/`
   把这个地址发给别人就能访问了（公开的谁都能开）。

> 更新站点：重新转换后把新的文件覆盖到仓库，`git add -A && git commit -m "update" && git push` 即可，Pages 会自动重建。

---

## 三、方式 B：Cloudflare Pages（免费，更快，国内访问相对友好）

### 前提
- Cloudflare 账号。
- 一个 zip 压缩包：把转换输出目录里的**所有文件**压成一个 zip（注意是整个目录内容，不是再套一层文件夹）。

### 步骤

1. 打开 https://pages.cloudflare.com → 点 **Create a project**。
2. 选 **Upload assets（直接上传文件）**（这个方式不用连 Git，最简单）。
3. 上传刚才打好的 zip → 填 project name，比如 `chm-site` → Save and Deploy。
4. 完成后给一个 `https://chm-site.pages.dev` 的地址，直接用。

> 之后要更新：删掉旧项目重建或重新上传 zip 覆盖即可（免费版不支持 Git 自动部署的上传方式，每次手动传一次）。

---

## 四、重要注意项

### 1. 中文 / 路径问题（最常踩的坑）
- 站点里很多 `.htm` 文件名可能是中文。GitHub Pages（多数静态托管）对中文文件名能正确处理，没问题。
- 但**不要在文件名里放空格和特殊符号**。若确有必要，可保留原样，一般也能行，只是链接偶尔会转义。

### 2. 入口就是 `index.html`
- 我们的转换产物**已经在输出目录根放了 `index.html`**，云端会自动当首页，所以直接传根目录即可，不必手动设置首页文件。

### 3. 公开/私密
- **GitHub Pages 免费版**：仓库公开 → 任何人都能访问站点；仓库私有 → 站点不发布（免费版）。
- **Cloudflare Pages**：默认公开。要私密还得配合访问控制（需要较新/商业套餐），一般团队直接用。
- 如果将来你的产品要「私密：只有特定人能看到」，这就是你要加 M2 可见性功能的时候，平台免费版暂时解决不了。

### 4. 域名
- 其实你已经有了 `xxx.github.io` / `xxx.pages.dev` 免费子域名，先不用急着买域名。
- 买了自己的域名后在平台「Domains / Custom domain」里绑一下即可。

---

## 五、一分钟验证清单

上线后把访问地址发到手机浏览器，逐项确认：

- [ ] 首页能打开（出现左侧目录、右侧内容区）
- [ ] 点左侧任意目录项 → 右侧能跳到对应章节
- [ ] 能折叠/展开目录（文件夹节点带 ▸/▾ 箭头）
- [ ] 用不同设备（手机/电脑）都能正常浏览

若某一步打不开，多半是「没等构建完成」或「目录根没放对」，回到对应步骤检查即可。

---

**一句话总结**：GitHub 建仓库 → 把输出目录推上去 → 打开 Pages 开关 → 拿到免费网址，完事。