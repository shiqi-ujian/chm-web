# CHANGELOG

> 由 agent 在每次反馈修复发布后维护（来源：QQ 群收集表 / 问题反馈入口）。最新在上。

## 2026-08-20

- **移动端体验优化（第二批）**：阅读壳「返回首页」改为回到站点根（兼容 GitHub Pages 子路径/离线导出）；打开目录抽屉自动滚动到当前章节；新增 Ctrl+K / Cmd+K 聚焦全库搜索；移动端顶栏隐藏重复的页内搜索按钮；首页/浏览页文档卡片整张可点击打开。`test-site` 增加阅读壳移动端结构断言，`npm test` 全绿。
- **私有文档阅读壳修复**：TOC/首页/正文 iframe 统一使用绝对 `/p/<id>/` 前缀，避免转私后 404；公开转私有后立即重建阅读壳；服务启动时自动重建存量私有阅读壳（旧壳前缀修复）；字号/排版/进度改为 iframe `load` 后再应用，移动端 A-/A 与进度按钮恢复可用。
- **私有文档全文搜索**：「我的文档」新增“搜索我的私密文档”，仅登录 owner 可见；分享链接访问者也可通过有效分享 token 在该文档内检索；搜索结果链接直达 `/p/<id>/...`。
- **分享链接管理增强**：「我的文档」分享按钮升级为“管理分享链接”，支持设置/更新有效期、撤销、查看当前链接；过期/撤销后的 token 立即失效。
- 附带兼容：`share_expires_at` 字段自动迁移，旧库无需手动处理；`npm test` 新增 `test-share-mgmt.js`。
- **部署运维补强**：`.deploy-tools/watchdog.sh` 探活（自动重启 + 连续失败 webhook/日志告警）、启动自检与旧库缺列启动崩溃修复。

### 待确认

（暂无）