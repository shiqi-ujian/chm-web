# CHANGELOG

> 由 agent 在每次反馈修复发布后维护（来源：QQ 群收集表 / 问题反馈入口）。最新在上。

## 2026-08-21

- **注册/账号体系补强**：
  - **登录支持邮箱或用户名**：忘记用户名也能用注册邮箱登录；
  - **注册人机校验**：注册前先取一道一次性算术题（`GET /api/auth-challenge`），提交注册时服务端校验，防批量刷注册；本地测试可用 `NO_CAPTCHA=1` 关闭；
  - **重发验证邮件冷却**：`/api/resend-verification` 加 60s 冷却与 24h 令牌过期管理，防刷信；
  - **修改绑定邮箱**：登录后在「我的文档 → 账号设置」改邮箱，改后自动进入未验证状态并重发验证邮件；
  - **自助注销账号**：登录后可删除账号与全部文档，后端软删用户并清理会话/用量/文档实体；前端「我的文档」新增入口；
  - `npm test` 全绿，`test-auth-v2` 增补邮箱登录/改邮箱/注销用例，`scripts/launch-check.js` 增加 `NO_CAPTCHA` 生产检查。

## 2026-08-20

- **邮件发信修复与增强（QQ SMTP 实测通过）**：
  - 修复 SMTP 三连坑：`send` 未定义 / DATA 缺少 RFC5322 `From` 头 / 邮件已发出但 Promise 未 resolve 导致 20s 超时误报；
  - 新增 `SMTP_FROM_NAME` 发件显示名，RFC2047 编码后收件人看到「CHM 网页 \<addr\>」而非裸地址。
- **登录强制邮箱验证**：绑定真实邮箱且未验证的账号禁止登录（legacy 无邮箱账号豁免）；注册成功前端不再误报「失败」，改为提示查收验证邮件；点击邮件验证链接后自动登录并跳回首页。
- **私有文档导出 ACL 修复（安全）**：`EXPORT_TOKEN` 未配置时匿名导出私有文档不再被放行，改为 403；仅文档 owner 或显式配置 token 可导出。
- **性能优化（大文档/弱网）**：
  - `parseHhc` 线性化：7000 节点目录从约 20s 降到 17ms；
  - 阅读壳目录树惰性渲染：默认只渲染顶层，展开分支才构建子节点，7000 节点初始 DOM 仅 17 个；
  - 转私有/公开不再卡死：壳重建跳过全量索引 + 实体迁移优先 `rename` + 全站重建异步合并触发；
  - 静态文本资源 gzip 压缩 + `Cache-Control`（公开 `public,max-age=300` / 私有 `private,max-age=300`，`sw.js` 强制 `no-cache`），弱网刷新不再“卡死”。
- **PWA 离线增强**：新增 `manifest.webmanifest` + Service Worker 增强，支持基本离线访问；测试全绿。
- **用量校准**：`user_usage` 启动时按 meta + 磁盘实际占用重算，修复“已用容量”失实；`test-quota` 增补漂移回归。
- **私密文档阅读壳修复（/p/ 前缀 + URL 归一化）**：TOC/首页/正文 iframe 统一使用绝对 `/p/<id>/` 前缀，转私有/公开后均重建阅读壳，服务启动时自动修复存量私有壳；新增 `normPath` 归一化，目录高亮/上一页/下一页/进度/URL 匹配恢复；字号/排版/进度改为 iframe `load` 后再应用，移动端 A-/A 与进度按钮恢复可用。
- **移动端体验优化（第二批）**：阅读壳「返回首页」改为回到站点根（兼容 GitHub Pages 子路径/离线导出）；打开目录抽屉自动滚动到当前章节；新增 Ctrl+K / Cmd+K 聚焦全库搜索；移动端顶栏隐藏重复的页内搜索按钮；首页/浏览页文档卡片整张可点击打开。`test-site` 增加阅读壳移动端结构断言，`npm test` 全绿。
- **私有文档阅读壳修复**：TOC/首页/正文 iframe 统一使用绝对 `/p/<id>/` 前缀，避免转私后 404；公开转私有后立即重建阅读壳；服务启动时自动重建存量私有阅读壳（旧壳前缀修复）。
- **私有文档全文搜索**：「我的文档」新增“搜索我的私密文档”，仅登录 owner 可见；分享链接访问者也可通过有效分享 token 在该文档内检索；搜索结果链接直达 `/p/<id>/...`。
- **分享链接管理增强**：「我的文档」分享按钮升级为“管理分享链接”，支持设置/更新有效期、撤销、查看当前链接；过期/撤销后的 token 立即失效。
- 附带兼容：`share_expires_at` 字段自动迁移，旧库无需手动处理；`npm test` 新增 `test-share-mgmt.js`。
- **部署运维补强**：`.deploy-tools/watchdog.sh` 探活（自动重启 + 连续失败 webhook/日志告警）、启动自检与旧库缺列启动崩溃修复。

### 待确认

（暂无）