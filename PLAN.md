# CHM → 可浏览/搜索网站 开发计划

> 定位：**个人 / 非营利** 项目。目标 = 用户上传 CHM → 转为**双端可浏览 + 可搜索**的子页；上传者可设为**私密（仅自己和被分享者可见）或公开**；后期支持逐章搜索；最终支持**批量私有部署导出**（把整个 CHM 转成自己可带走的站点/静态产物）。

---

## 技术选型（初判）

| 模块 | 选型 | 理由 |
|------|------|------|
| 运行环境 | `Node.js`（纯 JS，无框架依赖） | 后端服务 + 转换脚本；生产 Node ≥ 22 |
| CHM 解包 | `7z` / `mspack` / `chmlib`（解析 ITSF 头 + LZX 解压） | 现成开源，免造轮子 |
| 解包产物 | 静态 HTML + `.hhc` 目录树 + `.hhk` 关键字索引 | 7-zip 即能解出 |
| 双端体验 | 移动端/桌面端都用**响应式 HTML**，不引传统 CHM 浏览器 | 移动端只做“可读可搜”，桌面端再做“批量/部署” |
| 可见性 | “私有/公开/指定链接可见” → 登录态 + 邀请链接 | 简单 ACL |
| 全文检索 | 在线走 `SQLite FTS5` 服务端检索；离线/静态走 `site-index.json` | 后端 + 静态阅读层均可用 |
| 批量私有部署 | **导出产物 → 打 zip/jekyll 站点** | **后期核心卖点** |

## 里程碑

### M1 — CHM 解包 + 转静态 HTML（地基）
- [x] 输入 `.chm` → 输出 同名目录（内嵌 HTML + `.hhc` + `.hhk`）
- [x] 用 7-Zip 校验解包产物结构正确
- [x] 能正确画出 `.hhc` 目录树
- [x] 记录 `.hhk` 关键字，后续“站内搜索”用
- **验收**：`a.chm` → `a/` 目录可浏览首页（已用 7-zip.chm 跑通；`npm start` 或 `node bin/cli.js convert` 生成预览站）

### M2 — 双端可访问 + 可见性权限
- [x] 移动端/桌面端起本地静态服务，可翻页浏览（手机：顶栏+汉堡抽屉目录；桌面：左目录右侧正文）
- [x] 用户态与“可见性”绑定：**私密/公开/指定链接**（注册/登录/会话 `src/lib/auth.js` + `POST /api/register|login|logout`）
- [x] 上传条目挂在“我的上传”下，带上公开/私密标签（`test-auth.js` 32 项全绿：ACL / 分享链接 / 实体迁移 / 越权拒绝）
- **验收**：手机浏览器能打开子页面，公开条目免登录可见，私密条目需特定链接可见 ✅

### M3 — 全库检索（搜索）
- [x] 目录树检索（基于 `.hhc`）
- [x] 单文档全文检索（`.hhk` 关键字 + 基于 `.htm` 的浏览器端全文索引）
- [x] **跨文档 / 全站统搜**（欢迎页 `site-index.json` 聚合索引，一个框搜所有文档的关键字与正文）
- **验收**：搜索到 CHM 内页标题 / 关键字，手机端可用 ✅（已在欢迎页与阅读壳打通）

### M4 — 批量 + 私有部署（真正的“付费线”）✅ 已完成
- [x] 支持**一个/多个** CHM 批量上传（前端多选/拖拽 + 逐个上传汇总进度）
- [x] “**打包下载 / 导出为站点**”= 打 zip/静态站点（自托管，留住用户）——含**整站单包导出**与**选中多篇导出独立裸站 zip**
- **验收**：选中多个 CHM，一次转换后能整包导出为 zip/静态站点。✅

### M5 — 健壮与安全（白名单护栏）✅ 已完成
- [x] 原子写、会话过期清理、限流/配额、并发上传槽位、异步 7z + 超时 + 临时文件清理
- [x] 阅读壳/欢迎页存储型 XSS 修复、移除页面烘焙令牌、服务端检索 `/api/search`
- [x] SQLite 化（better-sqlite3 + FTS5）
- **验收**：`npm test` 全绿（15 项），公共服务具备基础防护。✅

### M6 — 阿里云生产迁移 ✅ 已完成
- [x] 生产环境从 Railway 迁至阿里云服务器，线上已搭好
- [x] 部署链路：`push 到 main → GitHub → 阿里云自动拉取/触发部署`（已验证）
- [x] 运维脚本入库：`.deploy-tools/`（deploy/backup/systemd/ssh）
- **验收**：代码推送到 main 后阿里云线上闭环可用。✅

### M7 — 登录加固 + 移动端 + 合规 ✅ 已完成
- [x] 注册强邮箱 + 同意条款；邮箱验证；忘记/重置密码；登录失败锁定；CSRF
- [x] 移动端汉堡菜单/阅读壳适配；上传权利勾选
- [x] 合规页 terms/privacy/disclaimer/report + 举报/下架/管理后台
- **验收**：`npm test` 15 项全绿，含 `test-auth-v2`。✅

---

## 开发阶段（建议顺序）

1. **P0·跑通**：`7z` 解包一个 CHM → 起静态服务 → 手机能开 → **约 3~5 天**
2. **P1·目录+搜索**：解析 **hhc/hhk** → 页面搜索 → **约 3~7 天**
3. **P2·可见性 + 账号**：登录态、私密/公开链接、主体** → **约 5~10 天**
4. **P3·批量导出/部署**：批处理 + 生成静态站点/zip（**真正的球门**）→ **约 7~14 天**

> **总计（单人业余）：约 3~6 周**。如果 90% 走基础（解包+静态托管），大约 2~3 周就能上线 MVP。

---

## 成本估算（当前阶段）

| 项目 | 方式 | 成本 |
|------|------|------|
| 域名 | 可先用你自己的 / 免费 `*.pages.dev` | ¥0（先期） |
| 托管 | GitHub Pages / Cloudflare Pages / 自有小服务器 | ¥0 ~ ¥20/月 |
| 解包 | 用 7-Zip / `mspack` | ¥0（一次性开源） |
| 存储 | 解包后都是 KB 级小文件 | 几乎可忽略 |
| 搜索 | 目录（hhc）= 静态；全文搜索进阶 | 初期 ¥0，后续如需可加 |
| **批量私有部署导出** | 自己生成 zip/静态站点 | **主要人工成本（你的维护时间）** |

**一句话结论：如果是个人 + 静态 + 基础托管，成本 ≈ 0 元/月；花钱主要在“批量/导出/私密部署”这些让你真正省事的功能上。**

---

## 关键风险与你对该计划的把握

| 这个计划里“站得住”的 | 真正会“烧钱” |
|----------------|----------------|
| 上传→解包→浏览（pc+手机） | 批量/导出/私有部署 |
| 公开/私密可见性 | 全文搜索 |
| 目录保留 | 长期维护（你本人的时间） |

---

## 最后，一开始我的建议

1. **先做 P0 (MVP)**：`7z` 解开一个 CHM → 起个静态服务 → 手机能开：真正体验“核心闭环”。
2. **再做可见性**：公开/私密 + 分享链接（快速见效的部分）。
3. **把“批量导出/私密部署”做成卖点**，那才是别人没法免费拿走的部分。

要不要我接着写：任何一批工程的 `PLAN.md` 阶段里程碑、验收标准都直接拿过去了，还是你指了一个更具体的方向？我可以接着往下推。

好——你桌面，已经准备了**开发文件夹**。按上面那一版`PLAN.md`，我们现在就有、按阶段上马的东西了。